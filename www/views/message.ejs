<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Send a Message</title>
    <link rel="stylesheet" href="css/normalize.css" />
    <link rel="stylesheet" href="css/styles.css" />
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #000000;
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      header {
        background-color: #333;
        color: white;
        padding: 10px;
        text-align: center;
        width: 100%;
      }
      .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
        max-width: 800px;
        background-color: #131313;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        margin-top: 20px;
        padding: 10px;
        /*I ADDED THIS*/
      }

      nav {
        background-color: #555;
        padding: 10px;
        width: 100%;
        transition: color 0.3s ease;
      }

      nav a {
        color: white;
        text-decoration: none;
        padding: 10px;
        margin: 0 10px;
      }
      button,
      input[type="submit"] {
        font-size: 1rem;
        background-image: linear-gradient(to top, #f77062 0%, #fe5196 100%);
        padding: 14px 32px;
        border: none;
        border-radius: 4px;
        color: #fff;
        cursor: pointer;
        transition: all 0.35s;
        outline: none;
      }

      button,
      input[type="submit"]:hover {
        background: #4837ff;
        transition: all 0.3s ease;
      }
      .whitetext {
        color: white;
      }
      .textarea {
        display: flex;
        align-items: center;
      }
      select {
        margin-left: 47px;
      }

      textarea {
        padding: 10px;
        margin: 10px 0;
        /*I ADDED THIS*/
        margin-left: 5px;
        width: calc(100% - 20px);
        border: 1px solid #333;
        border-radius: 4px;
        background-color: #333;
        color: snow;
        resize: none;
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Send a Message</h1>
    </header>

    <nav>
      <a href="/">Home</a>
    </nav>

    <section class="container">
      <% if(typeof user !='undefined'){ %>
      <h2 class="whitetext">Sending As:<%=user.email %></h2>
      <%}%>
      <form class="whitetext" action="/sendmessage" method="POST">
        <label for="emails">To:</label>
        <select id="emails" name="toemail">
          <%if(typeof users!='undefined'){%> <% for(let u of users){ %> <%
          if(typeof user != 'undefined' && user.email!=u.email) { %>
          <option value="<%= u.email%>"><%=u.email%></option>
          <%}%> <%}%> <%}%>
        </select>
        <div class="textarea">
          <label for="message">Message: </label>
          <textarea id="message" cols="25" rows="7" name="message"></textarea>
        </div>
        <div>
          <input type="submit" />
        </div>
      </form>
    </section>
  </body>
</html>
